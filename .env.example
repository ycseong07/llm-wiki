# llm_wiki — .env example
# 실제 비밀값은 Windows Credential Manager(keyring) 경유. .env 평문 비밀 금지.

# Obsidian 볼트 루트 (raw/articles/ 가 이 아래에 있어야 함)
OBSIDIAN_VAULT_PATH=C:\Users\samsung\Documents\obsidian_vault

# Gemini API 키 — 기본은 keyring. 임시 테스트일 때만 .env로 (커밋 금지).
# GEMINI_API_KEY=
